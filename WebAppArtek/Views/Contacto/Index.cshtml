@model IEnumerable<WebAppArtek.Models.Contacto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cont_Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cont_Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cont_Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cont_Estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Contacto }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Contacto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Contacto })
        </td>
    </tr>
}

</table>
