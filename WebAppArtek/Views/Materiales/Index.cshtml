@model IEnumerable<WebAppArtek.Models.Materiales>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Mat_Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mat_Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mat_Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Mat_Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mat_Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mat_Estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Materiales }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Materiales }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Materiales })
        </td>
    </tr>
}

</table>
